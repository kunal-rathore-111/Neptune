import Dashboard from "./Dashboard";

export default function SharedProfile() {
  /* rendering the dashboard page just changing the permissions of no edits possible bu isAuthenticated global state */
  return <Dashboard />;
}
